#include <iostream>
#include <Windows.h>
using namespace std;

int main()
{
    int number;
    if(number>0) {
        cout << "Це число додатнє"; 
    } else if(number<0) {
        cout << "Це число відємне";  
    } else {
        count << "Це число дорівнює нулю";  
    }
    return 0;
}
